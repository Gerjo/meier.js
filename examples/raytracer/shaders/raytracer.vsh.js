
define(function(require) {
    
    

    return function() {
        /*
            precision highp float;
        
            // Per vertex attributes:
            attribute vec2 attribPosition;
        
            // Per program uniforms:
            uniform vec2 windowSize;

            // Transfer to fragment shader:
            varying vec2 inPosition;

            void main(void) {
    
                float aspect = windowSize.x / windowSize.y;
        
                // Transfer unit screen location
                inPosition = attribPosition;
        
                // Adjust for screen aspect ratio
                //inPosition.y /= aspect;
        
                gl_Position = vec4(
                    attribPosition, 
                    0.0,        // Expand for 3D
                    1.0         // Expand for homogenious
                );
        
        
            }
        
        */
    };
});